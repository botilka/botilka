tools:
    external_code_coverage: true

build:
    node:
        my-tests:
            services:
                mongo: 4
            dependencies:
                before:
                - pecl install mongodb
