sudo: false
language: php

services:
  - mongodb
  - postgresql

php:
  - '7.1'
  - '7.2'

env:
  global:
    - POSTGRES_HOST=localhost
  matrix:
    - deps=low
    - deps=high

matrix:
  allow_failures:
    - env: deps=low

before_script:
  - composer global require friendsofphp/php-cs-fixer
  - export PATH="$PATH:$HOME/.composer/vendor/bin"

script:
  - vendor/bin/phpunit --coverage-clover=coverage.clover
  - vendor/bin/phpstan analyse
  - php-cs-fixer fix --allow-risky=yes --dry-run

after_script:
  - wget https://scrutinizer-ci.com/ocular.phar
  - php ocular.phar code-coverage:upload --format=php-clover coverage.clover

install:
  - pecl install mongodb && echo "extension = mongodb.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - if [[ $deps = 'low' ]]; then
      composer update --prefer-dist --no-progress --no-suggest --ansi --prefer-stable --prefer-lowest;
    else
      composer update --prefer-dist --no-progress --no-suggest --ansi;
    fi

cache:
  directories:
    - $HOME/.composer/cache
